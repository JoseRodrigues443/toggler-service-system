// import * as Stomp from "@stomp/stompjs"

export class ListenerAccessLayer {

    constructor() {
        // http://localhost:5672/ws
        // var ws = new WebSocket('ws://127.0.0.1:15674/ws');
        // var client = Stomp.over(ws);
    }
}